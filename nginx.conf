server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Hashed assets (immutable) - Cache for 1 year
    location ~* \/assets\/.*-[A-Z0-9]{8,}\.(js|css)$ {
        expires 1y;
        add_header Cache-Control "public, max-age=31536000, immutable";
        access_log off;
    }

    # Static images and files - Cache for 1 day (adjust as needed)
    location ~* \.(jpg|jpeg|png|gif|ico|webp|svg|woff|woff2|ttf|eot)$ {
        expires 1d;
        add_header Cache-Control "public, max-age=86400";
        access_log off;
    }

    # SPA Routing - For any other request, return index.html
    # Do not cache index.html so users always get the latest deployment ref
    
    # --- PRERENDER.IO SEO SETUP ---
    # To enable SEO for Bing/Yandex, uncomment the block below and replace YOUR_TOKEN.
    # set $prerender 0;
    # if ($http_user_agent ~* "googlebot|bingbot|yandex|baiduspider|twitterbot|facebookexternalhit|rogerbot|linkedinbot|embedly|quora link preview|showyoubot|outbrain|pinterest\/0\.|pinterestbot|slackbot|vkShare|W3C_Validator|whatsapp") {
    #     set $prerender 1;
    # }
    # if ($args ~ "_escaped_fragment_") {
    #     set $prerender 1;
    # }
    # if ($http_user_agent ~ "Prerender") {
    #     set $prerender 0;
    # }
    # if ($uri ~* "\.(js|css|xml|less|png|jpg|jpeg|gif|pdf|doc|txt|ico|rss|zip|mp3|rar|exe|wmv|doc|avi|ppt|mpg|mpeg|tif|wav|mov|psd|ai|xls|mp4|m4a|swf|dat|dmg|iso|flv|m4v|torrent|ttf|woff|svg|eot)") {
    #     set $prerender 0;
    # }
    # resolver 8.8.8.8;
    # if ($prerender = 1) {
    #     set $prerender_url "http://service.prerender.io/$scheme://$host$request_uri";
    #     rewrite .* /$prerender_url break;
    #     proxy_pass http://service.prerender.io;
    #     proxy_set_header X-Prerender-Token YOUR_TOKEN_HERE;
    # }
    # --- END PRERENDER.IO ---

    location / {
        try_files $uri $uri/ /index.html;
        expires -1;
        add_header Cache-Control "no-store, no-cache, must-revalidate";
    }

    # Cache broken sitemap/robots.txt queries just in case
    location = /index.html {
        expires -1;
        add_header Cache-Control "no-store, no-cache, must-revalidate";
    }
}
